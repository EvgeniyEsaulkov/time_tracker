<nav class='relative w-full flex flex-wrap items-center justify-between py-3 bg-gray-100 text-gray-500 hover:text-gray-700 focus:text-gray-700 shadow-lg'>
  <div class='container-fluid w-full flex flex-wrap items-center justify-between px-6'>
    <div class='container-fluid'>
      <%= link_to 'TimeTracker', root_path, class: 'text-xl text-black' %>
    </div>
    <div class='container-fluid'>
      <% if user_signed_in? %>
        <div class="relative" data-controller="user-dropdown">
          <div class="flex items-center">
            <% if current_user.admin? %>
              <%= link_to 'Invite user', new_invitation_path, class: 'text-sm text-black mr-2' %>
              <span class="text-sm text-black mr-2"> | </span>
            <% end %>
            <span class="text-sm text-black cursor-pointer" data-action="click->user-dropdown#toggle">
              <%= current_user.email %>
            </span>
          </div>
          <div class="dropdown-content hidden bg-white absolute right-0 top-full mt-2 w-40 border border-gray-300 shadow-lg" data-user-dropdown-target="content">
            <%= link_to 'Sign Out', destroy_user_session_path, data: { 'turbo-method': :delete }, class: 'block px-4 py-2 text-gray-800 hover:bg-gray-100' %>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</nav>
<br/>
